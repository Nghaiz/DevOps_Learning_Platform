{
  "decision": "PASS_WITH_RISK",
  "disprovenClaims": [
    "BÁC BỎ — plan F7 nói: '`page.tsx` (Server Component) … nạp `session-terminal.tsx` bằng `next/dynamic` với `ssr: false`'. Bản đó KHÔNG BUILD ĐƯỢC trên Next 16: `ssr: false` bị cấm trong Server Component (đọc `node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md`: '`ssr: false` is not supported in Server Components. You will see an error'). Tầng thật phải ba lớp: page (server, auth) → session-client (client, máy trạng thái) → terminal-pane (client, đụng document). Bác bỏ bằng tài liệu của chính version đang cài, không bằng trí nhớ.",
    "BÁC BỎ — contract §5 liệt kê `hardCapAt` là field bắt buộc của `ready`. KHÔNG BÊN NÀO GỬI NÓ, và gateway cố ý không gửi vì lý do đúng (mốc = createdAt + HARD_CAP, mà HARD_CAP là config của orchestrator; gateway tự tính = dựng hằng số thứ hai). Bác bỏ bằng cách đọc `buildReady` trong bridge.go rồi đối chiếu frame thật trên cụm.",
    "BÁC BỎ — giả định 'pod nằm trong pool:free thì pod đó còn sống'. Sai: sau khi VMware sập, `pool:free` giữ `sandbox-674a2a67af4a` với `pod:{name}.state=free` trong khi Kubernetes báo pod `Failed` (container terminated, exitCode 255). Bác bỏ bằng cách đọc CẢ HAI nguồn rồi so, không tin một nguồn."
  ],
  "missingProof": [
    "Không có ảnh/log nào cho nhánh fallback DOM renderer — cần một lượt chạy với hardware acceleration tắt.",
    "Không có phép đếm WebSocket sống dưới `<StrictMode>` trong `next dev`; cần jsdom + mock `terminal-core` hoặc một lượt dev thật.",
    "Không có số đo `expiresAt` trước/sau một cú bấm 'Gia hạn'.",
    "Không có phép đo p95 cho '101 → ready' ở đường FE (nợ này vốn đã có từ 1.C-3, chặng này không đóng)."
  ],
  "reachableRegressions": [
    "⛔ CHẶN NGƯỜI DÙNG (lane orchestrator, chưa vá ở mã) — pod `Failed` nằm lại trong `pool:free` và `claim.lua` sẽ phát nó cho sinh viên tiếp theo. `RestartPolicy: Never` ⇒ mọi lần node reboot đầu độc warm-pool; không tầng reaper nào phủ ca này (`IsTerminal()` chỉ được gọi ở `waitReady`, tức lúc TẠO). Với `POOL_TARGET=1` thì sinh viên ĐẦU TIÊN sau reboot nhận đúng pod chết. Đã dọn tay trên cụm; cần tầng sweep thứ 4 quét `pool:free` đối chiếu apiserver.",
    "`~/dlp-deploy` trên VM lệch repo 20h (thiếu toàn bộ khối env gateway gồm `REDIS_URL`) ⇒ `helm upgrade` từ nó làm gateway CrashLoop và release rơi `failed` (revision 20). Cơ chế: `--reset-then-reuse-values` giữ VALUES nhưng KHÔNG giữ TEMPLATE. Đã đồng bộ chart; bẫy vẫn còn cho lần sau nếu ai đó upgrade trước khi sync.",
    "Image sandbox không có `sudo` — quan sát, chưa phải lỗi (pod chạy user-ns có uid_map offset), nhưng một nền tảng DẠY DevOps mà bài lab gõ `sudo` là chết thì lane image nên quyết định có chủ ý.",
    "Cụm lab đang ở `betterAuthUrl=http://localhost:8080` khác mặc định chart (3000). Có chủ ý (không có ingress, contract §2 đòi cùng origin) nhưng là một khác biệt giữa lab và chart mà lần deploy sau phải biết."
  ],
  "unverifiedClaims": [
    "AC 'tắt hardware acceleration → fallback DOM renderer + console.warn' — CÓ CODE, CHƯA CHẠY.",
    "AC 'StrictMode dev: 3 lần mount/unmount → 1 WebSocket sống' — đo GIÁN TIẾP ở tầng connection, chưa đo vòng đời effect React thật.",
    "AC 'reconnect: ngắt mạng 5s → thấy đúng màn hình cũ' — backoff có test thuần; chưa ngắt mạng thật qua đường FE.",
    "Nút 'Gia hạn' (`session.extend`) chưa bấm trên cụm — chưa thấy `expiresAt` nhích lên bằng mắt.",
    "Ba theme đổi được ở dropdown nhưng chỉ `dlp-dark` được nhìn thấy trong ảnh."
  ]
}
