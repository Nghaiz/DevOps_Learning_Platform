# Rà soát toàn bộ P0 + P1 — 2026-08-10

**Bối cảnh:** sau đợt chỉnh sửa/nâng cấp gần nhất (PR #24→#28 + helm revision 8 deploy lúc 11:09 hôm nay).
**Nguyên tắc:** không tin ô tick trong plan — chạy lại từng cổng và đọc trạng thái cluster thật.
**HEAD:** `17fed14` · working tree sạch · `main == origin/main`.

---

## 1. Đo lại — mọi cổng đang XANH

| Cổng | Kết quả | Ghi chú |
|---|---|---|
| CI trên `main` | ✅ run `31358342543`, 13m01s, success | run trước đó (#27) đỏ ở job migrator — #28 đã vá |
| Go build + vet (`GOOS=linux`) | ✅ 4/4 module | ép `GOOS=linux` để không bị build tag giấu file `*_unix.go` |
| Go test | ✅ 0 FAIL, **0 SKIP** | xem §1.1 — con số này chỉ đúng khi có datastore thật |
| `buf lint` | ✅ exit 0 | |
| `buf breaking` vs `HEAD~1` | ✅ exit 0 | |
| `buf generate` → drift | ✅ 0 diff | `git status` rỗng sau khi sinh lại |
| `node scripts/env-check.mjs` | ✅ 46 biến, 4 scope | |
| `turbo run lint typecheck` | ✅ 9/9 task | |
| `turbo run test` (`--force`) | ✅ 64 web + 23 shared-types + 3 ui = **90**, 0 skip | |

### 1.1 Bẫy đã dính rồi gỡ — suite xanh vì skip sạch

Lần chạy đầu: `docker compose` đang TẮT ⇒ `go test ./...` exit 0 nhưng **81 test SKIP**
(`pool`, `lifecycle`, `reaper` đều đòi `REDIS_URL`). Sau khi `docker compose up -d` và
export `REDIS_URL`/`DATABASE_URL`: **0 SKIP**, vẫn xanh.

> Root `.env` chỉ có 6 biến của compose (`POSTGRES_*`, `REDIS_*`) — **không** có
> `REDIS_URL`/`DATABASE_URL`. Ai chạy `go test` mà không tự ghép hai URL đó sẽ nhận
> đúng một suite xanh không kiểm gì. Đây là cổng đang tự vô hiệu hoá theo mặc định.

### 1.2 Cluster thật (`192.168.94.130`)

- `helm list`: `platform` revision **8**, deployed 2026-08-10 11:09, namespace **`default`**.
- 5/5 pod nền tảng `Running` 113m; image `ghcr.io/nghaiz/dlp-*:sha-d5da78b`.
- Warm-pool sống: `dlp-sandbox/sandbox-20ad99c1d909` — `registry.k8s.io/pause:3.10`,
  `runtimeClassName=sysbox-runc`, `hostUsers=false`.
- ResourceQuota: used `pods:1, requests 500m/512Mi` trên hard `pods:10, requests 2100m/2112Mi`
  — **khớp chính xác D16** (trần hiệu lực 4 pod + phần canary).
- Metric B9 sống thật: `dlp_claim_duration_seconds{path="warm"|"cold"}`, `dlp_build_info`.

---

## 2. Tiến độ P1 theo thực tế mã nguồn

| Chặng | Trạng thái thật | Bằng chứng |
|---|---|---|
| 1.B0.1–1.B0.5 | ✅ xong (AC còn 2 ô — xem §3) | cron + PVC + JWKS + Caddy proxy đều có |
| 1.A-1 / 1.A-2 spike | ✅ HARD-GATE đã mở | report + `internal/pool/` |
| 1.B B1–B9 + B0′ | ✅ xong, 31 file Go, test thật xanh | metric sống trên cluster |
| **1.C gateway G1–G13** | ⛔ **chưa bắt đầu** | `internal/` chỉ có `config` (60 dòng) + `wsroute` (34 dòng); phần còn lại là `cmd/spike-exec` |
| **1.D** | ⛔ chưa | D-17′/D-21′/D-22′ là việc viết lại AC; D-19′ (`podPidsLimit`) chặn bởi Calico |
| **1.E image** | ⛔ **placeholder thuần** | `Dockerfile` = `FROM ubuntu:26.04` + `CMD bash`, 0 gói |
| **1.F FE** | ⛔ chưa | `packages/terminal/` **chỉ có README.md** |

Đường găng còn lại: **1.E-1 phải chen sớm** (warm-pool đang tạo pod `pause`, không phải image lab)
rồi **1.C** song song **1.F**.

---

## 3. Hai AC của 1.B0 chưa đóng được

**3.1 — Calico (chờ đồng hồ, KHÔNG chờ việc).**
Cron tạo lúc `2026-08-09T07:40:14Z`. Vậy:

| Mốc | Thời điểm (+07) | Trạng thái |
|---|---|---|
| T+22h (bây giờ) | 2026-08-10 13:03 | đang chạy |
| **T+25h** | **2026-08-10 15:40** | còn ~2.5h |
| **T+37h** | **2026-08-11 03:40** | còn ~14.5h |

Vế "≥2 lần restart thành công" **đã đạt**: 2 Job `dlp-calico-token-refresh` Completed
(17h trước và 5h59m trước).

> ⚠️ **BẪY vừa phát hiện — bằng chứng sẽ tự bốc hơi.**
> `dlp-cni-canary` có `successfulJobsHistoryLimit: 1`. Nhịp cron là 30 phút ⇒ **chỉ giữ đúng
> một Job**. Bằng chứng canary tại T+25h (15:40 hôm nay) sẽ bị xoá **trước khi** tới T+37h,
> nên không thể chứng minh cả hai mốc từ trạng thái cluster. Hoặc nâng history limit **ngay
> bây giờ** (26 ≈ 13h lịch sử), hoặc phải thức canh lúc 03:40 sáng mai.

**3.2 — "web và gateway cùng origin"** — chờ G1 + G12 đúng như plan ghi. Topology đã chứng minh
(handshake qua Caddy trả 101), phần nợ chỉ là cookie `dlp_sandbox` chưa ai mint.

---

## 4. Nợ P0 — rà lại thực tế, có một mục plan ghi SAI

| ID | Plan ghi | Thực tế đo hôm nay |
|---|---|---|
| R4 | Còn mở (cookie refresh path `/api/auth`) | ✅ đúng, còn mở, cố ý |
| R6 | Còn mở, P3 (`securityContext` 3 Deployment nền tảng) | ✅ đúng — `grep securityContext` trên `web/orchestrator/gateway-deployment.yaml` ra **0 kết quả** |
| **R7** | **"Còn mở, P3" — action chưa pin SHA** | ❌ **SAI — đã đóng rồi.** `21/21` dòng `uses:` trong `ci.yml` đều pin SHA 40 ký tự. Plan cần cập nhật |
| R8 | `enforce_admins: false` cố ý | ✅ đúng |

---

## 5. Nợ / điểm lệch MỚI phát hiện trong đợt rà này

| # | Phát hiện | Mức | Ghi chú |
|---|---|---|---|
| N1 | **Trivy chạy SAU `push: true`** ⇒ image có CRITICAL **vẫn được publish**, cổng chỉ làm run đỏ chứ không chặn artifact | Cao | #28 đã nêu hiện tượng nhưng không sửa thứ tự |
| N2 | **Namespace thật là `default`, không phải `dlp-platform`.** SA là `platform-gateway`/`platform-orchestrator` | Cao | Verify command `phase-1.md:447-450` dùng `system:serviceaccount:dlp-platform:dlp-platform-gateway` ⇒ chạy sẽ ra **"no" giả**. R24 ("NetworkPolicy cho namespace platform") cũng không áp dụng được khi platform nằm chung `default` |
| N3 | Cluster đang chạy `sha-d5da78b` (#27) — **chưa có #28** | Trung bình | Image migrator trên cluster vẫn là bản 3 CVE CRITICAL |
| N4 | `pnpm format:check` đỏ **61 file** | Thấp | Cố ý, `ci.yml:169` ghi rõ chưa bật cổng |
| N5 | migrator còn **2 HIGH** — binary Go của TS7 native lọt vào image qua stage `deps` | Thấp | Cổng chỉ chặn ở CRITICAL |
| N6 | Issue **#20** mở: gỡ `useTypeScriptCli:false` + alias TS6/TS7 | Thấp | Chờ typescript-eslint hỗ trợ TS ≥ 7.1 |
| N7 | Root `.env` thiếu `PROXY_PORT` so với `.env.example` | Rất thấp | Có default `8080` |
| N8 | **Không có `make` trên Windows** ⇒ mọi verify command dạng `make ...` trong plan không chạy được ở máy dev | Trung bình | Chạy trực tiếp `go` / `buf` / `node` thì được — plan nên ghi đường thay thế |

---

## 6. Doc drift cần vá trong plan

1. `phase-0.md` R7 → đánh dấu **ĐÓNG** (21/21 action đã pin SHA).
2. `phase-1.md` §"Còn để ngỏ": mục **"Migration Postgres chưa tự động hoá"** đã được B8 đóng
   (image `migrator` + Job Helm hook) nhưng bullet vẫn ghi là nợ.
3. `phase-1.md` §"Còn để ngỏ": **`SESSION_TTL` đã chốt `1h`** ở B5–B9 nhưng bullet vẫn ghi "chưa pin".
4. `phase-1.md` §1.B: "B5–B9 (PR tiếp theo)" → đã là **PR #27**.
5. `phase-1.md:447-450`: sửa SA/namespace trong verify command RBAC về `default:platform-*`.
6. `phase-1.md` R24: mô tả lại theo namespace `default` thật (hoặc quyết định tách namespace).

---

## 6b. Đợt hai — đóng nợ bằng chứng của 1.B (chiều 2026-08-10)

Hook `workflow-artifact-gate` cảnh báo trỏ vào artifact PR #27. Chẩn đoán: **cả hai giả thuyết đều đúng, không ngang nhau.**

**Con trỏ artifact:** hook chọn thư mục artifact *mới nhất trên đĩa*; đợt rà này chưa sinh artifact nên nó rơi về `2026-08-09-p1-b-b5-b9`. Còn "review chưa PASS" là do [`validator.cjs:147`](../../.claude/hooks/workflow-artifact-gate/validator.cjs) đòi `decision === 'PASS'` chặt chẽ ở hard stage, trong khi artifact ghi `PASS_WITH_RISK` / `score 8` / `criticalCount 0` / `blockingReasons: []`. Đó là lỗi ĐỌC, không phải review đỏ. Đã sinh artifact cho đợt này: [`harness/2026-08-10-audit-p0-p1/`](../devops-learning-platform/reports/harness/2026-08-10-audit-p0-p1/).

**Nhưng nợ bằng chứng là thật.** Ba mục nặng, đã đóng bằng số đo:

| Nợ | Kết quả |
|---|---|
| Reaper chưa từng chạy trên cluster | ✅ **tầng 2a**: pod mồ côi thật — im lặng 340s (tôn trọng `orphanGrace`), xoá đúng một chu kỳ sau đó, `orphan_pods_total` 0→1, warm-pool nguyên vẹn. ⚠️ tầng 1/2b/2c/3 **vẫn chưa** |
| Job migration chưa qua `helm install` thật | ✅ và **lôi ra lỗi CHẶN** — xem dưới |
| AC claim p95 ≥ 50 mẫu | ✅ **p95 = 0.090s / 50 mẫu warm** |

**Lỗi chặn phát hiện được:** Job migrate mang `hook: pre-install`, mà hook `pre-install` chạy trước toàn bộ pha apply thường ⇒ lúc Job khởi động, Postgres **chưa tồn tại** (`kubectl get deploy,svc` → `No resources found`). Fresh install + Postgres của chart **không bao giờ** chạy được. Sống sót vì mọi lần deploy đều là `upgrade` lên Postgres có sẵn, và `helm template` render giống hệt ở cả hai ca nên không thể thấy. Đã vá `post-install,pre-upgrade`, chứng minh cả hai đường trên cluster.

**Bốn lỗ hổng mã đã vá:** M-6 (đếm lặp pod `Terminating` — tái hiện được sau khi sửa test double, có kiểm đột biến), `rest.Config.Timeout=30s`, `StreamDenyInterceptor`, và cổng ràng buộc `orphanGrace ↔ pool.DefaultReadyTimeout`.

**Một tự-phát-hiện đáng ghi:** bản đầu của `cmd/bench-claim` in "XANH — đạt AC ≥ 50 mẫu" trong khi chỉ có **25** mẫu warm, và dòng "cold-path: 0" đếm nhầm khái niệm. Chính công cụ đo AC tự cho điểm sai. Đã vá: cổng số-mẫu đặt **trước** cổng p95, số cold lấy từ histogram nhánh cold của server.

**Còn nợ sau đợt hai:** proof runtime cho tầng 1/2b/2c/3 của reaper · độ dài cửa sổ web-trả-500 sau `helm install` mới (do chính bản vá hook sinh ra) · p95 phải đo lại sau 1.E · 4 bản vá mã + bản vá Helm **chưa deploy** (cluster đang chạy `sha-d5da78b`).

## 7. Kết luận — sẵn sàng cook 1.C/1.E chưa?

**Rồi, với một điều kiện.** Không có gì đang hỏng; mọi cổng xanh và đã được kiểm bằng số đo chứ
không bằng ô tick. Hai AC còn treo (`Calico`, `cùng origin`) **không chặn** việc bắt đầu 1.C/1.E:

- AC Calico chỉ chờ đồng hồ — nhưng **phải nâng `successfulJobsHistoryLimit` trước 15:40 hôm nay**,
  nếu không bằng chứng T+25h mất và AC phải đo lại từ đầu.
- AC "cùng origin" theo thiết kế là do G1+G12 đóng — tức nó nằm *bên trong* chặng sắp cook.

Việc duy nhất nên làm **trước** khi cook: nâng history limit của canary (5 phút) và chốt hướng
xử lý N2 (namespace) — vì G13 (test IDOR) và R24 đều tham chiếu namespace đó.
