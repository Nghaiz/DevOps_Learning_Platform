# `packages/scenario` — chỗ dành sẵn

Parser scenario Katacoda/Killercoda (markdown + `index.json`). Thuộc **P2 — trụ cột
Lessons** ([phase-2.md](../../plans/devops-learning-platform/phase-2.md)).

Chưa có `package.json` nên pnpm workspace bỏ qua thư mục này — đó là chủ ý.
