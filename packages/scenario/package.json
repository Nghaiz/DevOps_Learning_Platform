{
  "name": "@devops-platform/scenario",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//exports": "`.` kéo theo loader + source đọc đĩa (`node:fs`). `./content-blocks` là subpath AN TOÀN CHO TRÌNH DUYỆT — thuần chuỗi, không import Node built-in nào. Trang bài học chạy `parseContentBlocks` ở client, và import nó qua `.` làm Turbopack lôi `node:fs/promises` vào bundle trình duyệt rồi ĐỔ BUILD (`the chunking context does not support external modules`). Cùng lối với `@devops-platform/shared-types/scenario`. `./lab-score` cung la subpath AN TOAN CHO TRINH DUYET vi cung ly do: trang /labs/[id] cham cac ham tinh diem o client, va import chung qua `.` keo `node:fs/promises` vao bundle trinh duyet roi DO BUILD (da dinh that 2026-09-04 — typecheck/lint/test deu xanh, chi `next build` do).",
  "exports": {
    ".": "./src/index.ts",
    "./content-blocks": "./src/content-blocks.ts",
    "./lab-score": "./src/lab-score.ts",
    "./quiz-score": "./src/quiz-score.ts",
    "./path-progress": "./src/path-progress.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@devops-platform/shared-types": "workspace:*",
    "zod": "^4.4.3"
  },
  "//typescript": "Alias TS6/TS7 song song — xem giải thích ở package.json gốc: `tsc` = TS7 native, `typescript` = shim TS6 cho typescript-eslint API.",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.4.0",
    "@typescript/native": "npm:typescript@^7.0.2",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "typescript-eslint": "^8.68.0",
    "vitest": "^4.1.11"
  }
}
