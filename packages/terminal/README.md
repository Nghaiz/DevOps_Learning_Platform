# `packages/terminal` — chỗ dành sẵn

Wrapper xterm.js: WebGL addon, truecolor 24-bit, Nerd Font, theme switch, đồng bộ
resize với PTY (design §4b). Thuộc **P1**, cùng lúc terminal-gateway nối được pod.

Chưa có `package.json` nên pnpm workspace bỏ qua thư mục này — đó là chủ ý.
