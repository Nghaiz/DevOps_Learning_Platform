{
  "name": "@devops-platform/shared-types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//exports": "Subpath `./scenario` KHÔNG phải tiện nghi: barrel `.` kéo theo gen/orchestrator/v1/session_pb.ts, và file đó có `enum` — thứ Node strip-only từ chối (ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX). Mọi script chạy bằng `node` trần mà chạm DTO scenario (packages/scenario/scripts/parse.mjs) sẽ chết ở đó. Parser cũng vốn không cần type proto, nên subpath này vừa sửa lỗi vừa đúng phân tầng.",
  "exports": {
    ".": "./src/index.ts",
    "./scenario": "./src/scenario.ts",
    "./orchestrator/v1": "./gen/orchestrator/v1/session_pb.ts",
    "./lab": "./src/lab.ts",
    "./playground": "./src/playground.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.14.0",
    "zod": "^4.4.3"
  },
  "//typescript": "Alias TS6/TS7 song song — xem giải thích ở package.json gốc: `tsc` = TS7 native, `typescript` = shim TS6 cho typescript-eslint API.",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.4.0",
    "@typescript/native": "npm:typescript@^7.0.2",
    "eslint": "^10.9.1",
    "eslint-config-prettier": "^10.1.8",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "typescript-eslint": "^8.68.0",
    "vitest": "^4.1.11"
  }
}
