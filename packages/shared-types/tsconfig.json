{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "allowImportingTsExtensions": true,
    // Chỉ test cần Node API (đọc vector dùng chung từ docs/); code sản phẩm thì không.
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "gen/**/*.ts", "eslint.config.mjs"]
}
