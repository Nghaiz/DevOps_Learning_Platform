# P16 runtime acceptance — 2026-09-13

> **PARTLY HISTORICAL — the landing exercised here was replaced later the same day.** Build
> `HZG3aU7v-lBMcBxxAFsSE` carries the non-3D landing of 2026-09-12. The owner then reversed that
> direction and required a full-page scrolling 3D journey (`d5bf768`), verified separately at
> 20/20 on build `I7eYNXwmu3q9KzrnjDpAr`. The landing cases in this 171-case run therefore
> describe superseded source and are not acceptance for the current landing; results for the
> other 31 screens still stand. Current landing:
> [2026-09-13-landing-3d-completion.md](2026-09-13-landing-3d-completion.md).

**Complete: 171 passed, 0 failed, 0 skipped, 0 retries**, 374.3 seconds. Production build: `HZG3aU7v-lBMcBxxAFsSE`.

Evidence: [summary.json](harness/2026-09-12-p16-runtime/acceptance-all-green/summary.json), [full Playwright report](harness/2026-09-12-p16-runtime/acceptance-all-green/results.json), [declared target](harness/2026-09-12-p16-runtime/run-target.json).

| Suite | Passed | Failed | Skipped |
|---|---:|---:|---:|
| Accessibility | 36 | 0 | 0 |
| CSP | 37 | 0 | 0 |
| Keyboard | 44 | 0 | 0 |
| Landing visual and interactions | 4 | 0 | 0 |
| Progress motif and reduced motion | 4 | 0 | 0 |
| SMTP password reset | 1 | 0 | 0 |
| Performance | 4 | 0 | 0 |
| Responsive | 41 | 0 | 0 |
| **Total** | **171** | **0** | **0** |

## Target and runtime

Every Playwright invocation declared `E2E_START_SERVER=1`, `E2E_BASE_URL=http://localhost:3000`, `E2E_ORIGIN=http://localhost:3000`, `E2E_REQUIRE_ROLES=1`, and `E2E_REQUIRE_SESSION=1`.

Final command: `node apps/web/e2e/.artifacts/runtime/run-playwright.mjs acceptance-all-green a11y.spec.ts csp.spec.ts responsive.spec.ts keyboard.spec.ts motif.spec.ts perf.spec.ts password-reset.spec.ts landing-visual.spec.ts`. Private credentials were removed during cleanup; rerunning requires a fresh runtime and credentials.

Next served the verified production build on `127.0.0.1:3001`. The E2E ingress at `localhost:3000` forwarded actual terminal traffic to `127.0.0.1:8082`, preserving Origin and cookies. `reuseExistingServer:false` prevented reuse of another server. Existing local PostgreSQL and Redis were started; the database already held ten published problems, `K8S-0001` through `K8S-0010`, and no duplicate seed was added.

Global setup verified the dedicated test account's admin role from the server. Anonymous cases cleared cookies; protected routes used admin, which includes author access. Fresh local orchestrator/gateway binaries connected to the real Kubernetes node `debian-sandbox`, `192.168.94.130`. Real Sysbox sandboxes ran in isolated namespace `dlp-e2e-p16`, with quota and the existing sandbox network policies. Redis database 13 was verified empty before reservation; databases 14 and 15 were retained. Mailpit accepted real local SMTP on 1025, with the inbox on 8025.

## Final observations

- **32 routes have zero axe violations of any severity**, including both formerly unreachable problem detail routes. All 32 CSP checks passed, alongside controls proving inline script, foreign image and iframe content are blocked.
- Keyboard checks cover ten screens and sample the first 18 Tab stops per screen. Native Tab/Enter also verifies search open/input/close behavior and focus restoration.
- Real terminal evidence records one single-Escape byte, two rapid-pair bytes, and focus outside the terminal after the rapid pair. A pair separated by more than 500ms retained focus. Both cases required READY and ended their own sessions. [Gateway and orchestrator events](harness/2026-09-12-p16-runtime/acceptance-all-green/terminal-service-events.json) show real pod exec, WebSocket connections and reaping.
- The reset flow created one disposable `@example.test` account, requested the email through the browser, read the delivered Mailpit code, reset the password, verified the old session was revoked, and signed in with the new password. Codes were absent from URLs and not logged. The message and account were removed; no external recipient was used.
- All 32 screens passed the 390px overflow sweep. Other responsive checks cover 375, 768, 769, 1023, 1024 and 1280px boundaries.

## Landing and images

The user retired the old ring and 3D scene. Four final cases target the replacement HTML landing and Linux/Docker/Kubernetes example at 1440px/390px in light/dark themes. They verify all topics, run/reset, native theme changes, reduced motion on mobile and no horizontal overflow. Hero/full-page captures return to scroll zero and wait two animation frames.

Final images were visually reviewed: readable content and command output, no old scene, and the mobile sticky header at the page top. Captures are under `harness/2026-09-12-p16-runtime/acceptance-all-green/test-results/`:

- [Desktop light](harness/2026-09-12-p16-runtime/acceptance-all-green/test-results/landing-visual-landing-—-1-b13fd-ve-preview-and-theme-change-chromium/home-1440-light-full.png)
- [Desktop dark](harness/2026-09-12-p16-runtime/acceptance-all-green/test-results/landing-visual-landing-—-1-5e8c2-ve-preview-and-theme-change-chromium/home-1440-dark-full.png)
- [Mobile light](harness/2026-09-12-p16-runtime/acceptance-all-green/test-results/landing-visual-landing-—-3-c1bb8-ve-preview-and-theme-change-chromium/home-390-light-full.png)
- [Mobile dark](harness/2026-09-12-p16-runtime/acceptance-all-green/test-results/landing-visual-landing-—-3-5ee3b-ve-preview-and-theme-change-chromium/home-390-dark-full.png)
- [Real terminal after Escape Escape](harness/2026-09-12-p16-runtime/acceptance-all-green/test-results/keyboard-D10-—-thoát-termi-07688-à-Esc-Esc-rời-khỏi-terminal-chromium/terminal-escape.png)

## Performance and limits

| Route | LCP | TTFB | Budget |
|---|---:|---:|---:|
| `/` | 132ms | 15ms | 2500ms |
| `/lessons` | 584ms | 39ms | 2500ms |

Injecting 2000ms of document delay increased measured LCP by 2348ms. These are local Chromium production-build measurements, not cluster latency figures.

Axe marked `color-contrast` incomplete on eight routes: four auth pages, `/lessons`, `/labs`, `/quiz/dlp-quiz-docker-co-ban`, and `/games`. These are unresolved automated measurements, not violations. Token contrast checks and visual review remain separate evidence. This run does not certify all WCAG requirements, screen-reader reading order, hidden dialog states, non-text contrast or Tab stops beyond the sampled 18. Local gRPC used loopback development mode `GRPC_MTLS_MODE=off`; deployed cluster mTLS was outside this run.

E2E TypeScript checking, scoped ESLint and formatting passed. Env-check passed with 95 variables across four scopes. Code coverage was not collected by this browser lane.

## Defects found and closed

The earlier full run on `hVxgvKqAtSvzBnAe2XIOu` had **157 passed, 8 failed, 0 skipped**; its `acceptance-core-final/` archive is superseded by the final green run.

- Two catalog focus failures exposed an invisible vendor search close button remaining in the Tab order. The wrapper now synchronizes inert/aria-hidden state and restores focus. Original pixel assertions and a new native keyboard regression pass.
- Two terminal failures were the harness READY selector matching both badge and pill. It now targets the unique polite live region. A targeted real D10 rerun passed 3/3, then the entire final suite passed.
- Four author screens overflowed at 390px because tab rows did not wrap. Wrapping and touch-target fixes pass all original cases without changing overflow tolerance.
- Raw axe reports exposed moderate heading issues on 15 routes. Native catalog headings and optional CardTitle/AlertTitle levels now match their hierarchy. The semantic gate explicitly blocks missing h1/skipped levels; a real malformed HTML control proves enforcement.

Old 3D captures are historical only. The initial zero-test attempt during an interrupted concurrent build is marked `acceptance-core/NOT-RUN.md`; stale files from that attempt are not evidence.

## Cleanup

Both final sessions were reaped. The dedicated supervisor/Go processes were stopped, namespace `dlp-e2e-p16` deleted, and no listeners remain on 8081/8082/8083/9090. Redis database 13 had six lane-owned keys and is now empty. The dedicated admin account was deleted; disposable reset accounts and Mailpit messages both number zero. Private environment/auth-state files were removed. Existing PostgreSQL, Redis and Mailpit remain healthy; existing users, content and other Redis databases were retained. [Cleanup record](harness/2026-09-12-p16-runtime/acceptance-all-green/cleanup.json).

No commits were created by this lane. No required browser acceptance remains pending.
