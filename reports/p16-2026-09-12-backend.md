# P16 backend debt closure — 2026-09-12

## Password reset

Completed SMTP delivery backend, actual forgot/reset form actions, input/pending/failure/success handling, and local Mailpit configuration. Production configuration is documented in `docs/env/06-password-reset-smtp.md`; Helm accepts a separate SMTP Secret and provider-specific SMTP egress under platform network isolation. No production provider credentials existed in the local environment, and no emails were sent to real users.

Security behavior:

- Email code entered through a POST body; framework URL-code callback and reset query flow rejected.
- 15-minute expiry, hashed verification identifiers, one-time consumption, invalidation of older codes after reset.
- All Better Auth sessions and platform refresh tokens revoked after reset; browser session and refresh cookies expired.
- SMTP connection check runs behind Better Auth's rate limiter and origin middleware, and behind explicit body validation in the before hook.
- Missing provider/outage gives the same 503 for known and unknown accounts. Recipient-specific sending runs through Next `after()`; its failure is logged without secrets and cannot change the account-enumeration-safe response.
- UI confirms request acceptance, never claims mail reached the inbox. Abrupt process termination can interrupt an accepted send; no durable queue is claimed.

## Fresh verification

- `pnpm --filter @devops-platform/web exec tsc --noEmit`: passed.
- SMTP/Postgres/Better Auth integration + actual form tests + rule-08 + shell copy gate: **4 suites, 21 passed, 0 skipped**, final run at 23:52 local.
- The integration uses a real loopback SMTP socket and Postgres, not a mocked mail sender. It tests success, expiry, replay, old/new password validity, reset cookie expiry, refresh revocation, provider outage, recipient rejection privacy, malformed payloads, and real framework throttling without additional SMTP connections.
- Docker Mailpit `axllent/mailpit:v1.29.4` pulled and started on loopback ports 1025 and 8025. Browser acceptance through the local production build is owned by the runtime lane.
- Helm lint passes with and without the SMTP Secret; template output contains all six explicit SMTP Secret references.
- Full build/review/final acceptance are owned by the lead; these focused results do not replace them.

## P15: historical debt already completed

Commits `5d90ad1` and `6d8d2fc` implement P15 option B. `plans/devops-learning-platform/reports/2026-09-10-verify-p15.md` includes actual cluster quota/pod identity measurements, browser error text, and the load experiment. It explicitly says the original CPU-load failure threshold was not reproducible and does not claim a new measured threshold.

Fresh regression run in this session: `setup-background.test.ts`, `setup-plan.test.ts`, and `labs-start-attempt-setup.test.ts` — **3 suites, 50 passed, 0 skipped**. These test setup launch/reap ordering and occupied quota slots, stderr propagation, and refusal to grade while the background setup sentinel is not ready. No new P15 implementation was needed. No new cluster load curve is claimed by this lane.

## Coordination

The reset callback now calls the lead-owned `revokeUserCredentials` primitive, which serializes against refresh/bootstrap issuance through a common users-row lock. The lead owns its concurrency regression suite. Copy is owned by the copy lane. This lane made no commits and ran no full build.

Final validation after shared revoker wiring: scoped ESLint passed; auth/reset files have no TypeScript errors. A simultaneous landing-page rewrite introduced unrelated missing home copy keys during the final global typecheck, so the lead must rerun the complete typecheck after that lane stabilizes. SMTP NetworkPolicy rendered successfully with a configured provider CIDR and port 587.
