"""Chương trình nhỏ nhất đủ để chứng minh image bạn dựng CHẠY được.

Tệp này được nền tảng đẩy vào pod trước khi bài bắt đầu — bạn không phải gõ nó.
Ở bước 4 bạn sẽ COPY nó vào image của mình.
"""

import platform

print("DLP docker lab: xin chao tu container")
print(f"python={platform.python_version()}")
