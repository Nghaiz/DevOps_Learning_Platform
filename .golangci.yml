version: '2'

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gosec
    - misspell
    - revive
    - unconvert
  exclusions:
    generated: lax
    paths:
      - proto/gen

formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    paths:
      - proto/gen
