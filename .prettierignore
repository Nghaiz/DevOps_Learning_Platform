node_modules
.turbo
dist
.next
coverage
pnpm-lock.yaml

# Generated từ proto/ — SSOT là file .proto, không format tay.
packages/shared-types/gen
proto/gen

# Generated bởi drizzle-kit — format tay thì lần regenerate sau lại lệch.
apps/web/drizzle

# Nội dung vendor NGUYÊN VĂN từ upstream. `pnpm format` sẽ canh lại các
# `index.json` (upstream prolug dùng thụt 4 khoảng trắng) và ngay lập tức làm
# `node scripts/vendor-scenarios.mjs --check` báo LỆCH với commit đã ghim — tức
# một lệnh format vô hại phá luôn cổng chống drift VÀ lời khai license.
# `dlp.json` là file của TA nên vẫn được format.
content/scenarios/**
!content/scenarios/*/dlp.json

# Prettier ở repo này CHỈ lo TS/JS/JSON/YAML (phase-0.md 0.A task 4 ghi rõ "Prettier (TS)").
# Markdown để nguyên: prettier canh lại bảng và bẻ dòng văn xuôi tiếng Việt, tạo diff
# ồn ào khắp plans/ và docs/ mà không đổi nội dung gì.
**/*.md
**/*.html

# Script bootstrap host chạy trên Debian; cloud-init.yaml được cloud-init parse,
# không phải người đọc. Không để prettier canh lại thứ nó không sở hữu.
infra/host

# Template Helm KHÔNG phải YAML: `{{- if ... }}` ở đầu file làm parser YAML của
# prettier ném SyntaxError ("Block collections are not allowed within flow
# collections"). Đây là lý do `pnpm format:check` đỏ trên main trước khi có dòng
# này — nó chưa bao giờ là cổng CI nên không ai thấy. Chỉ ignore thư mục
# templates/; values*.yaml là YAML thật và vẫn được format.
infra/helm/platform/templates
